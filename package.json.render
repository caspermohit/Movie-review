{
  "name": "movie-review-render",
  "version": "1.0.0",
  "description": "Movie Review Application Backend - Render Deployment",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install && npm run build"
  },
  "engines": {
    "node": ">=18"
  }
} 